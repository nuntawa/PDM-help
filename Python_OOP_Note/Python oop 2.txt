Modify Properties
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

p1 = Person("Tobias", 25)
print(p1.age)

p1.age = 26
print(p1.age)

Delete Properties
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

p1 = Person("Linus", 30)

del p1.age

print(p1.name) # This works
# print(p1.age) # This would cause an error !!!!!!!!!!!!!!!!


class Person:
  species = "Human"  # Class property

  def __init__(self, name):
    self.name = name  # Instance property

p1 = Person("Emil")
p2 = Person("Tobias")

print(p1.name)#Emil
print(p2.name)#Tobias
print(p1.species)#Human
print(p2.species)#Human


class Person:
  lastname = ""

  def __init__(self, name):
    self.name = name

p1 = Person("Linus")
p2 = Person("Emil")

Person.lastname = "Refsnes"#ถ้าเปลี่ยน property ของ class property ของ  Instance  ก็จะเปลี่ยนตาม !!!

print(p1.lastname)#Refsnes
print(p2.lastname)#Refsnes


class Person:
  lastname = ""

  def __init__(self, name):
    self.name = name
  
  def change_last_name(self,new_last_name):
  	self.lastname = new_last_name
  	

p1 = Person("Linus")
p2 = Person("Emil")

Person.change_last_name(p1,'Refsnes')#เรียก Method โดยใช้ Class ใส่  instance เป็น  params แรก ด้วย
Person.change_last_name(p2,'Refsnes2')

#หรือ
#p1.change_last_name('Refsnes')
#p2.change_last_name('Refsnes2')
#ก็จะได้ผลลัพธ์ เหมือนกัน

print(p1.lastname)
print(p2.lastname)


class Person:
  def __init__(self, name):
    self.name = name

p1 = Person("Tobias")
p2 = Person("Tobias2")

p1.age = 25
p1.city = "Oslo"

print(p1.name)
print(p1.age)
print(p1.city)

print(p2.city)#จะ Error !!!!!


class Playlist:
  def __init__(self, name):
    self.name = name
    self.songs = []

  def add_song(self, song):
    self.songs.append(song)
    print(f"Added: {song}")

  def remove_song(self, song):
    if song in self.songs:
      self.songs.remove(song)
      print(f"Removed: {song}")

  def show_songs(self):
    print(f"Playlist '{self.name}':")
    for song in self.songs:
      print(f"- {song}")

my_playlist = Playlist("Favorites")
my_playlist.add_song("Bohemian Rhapsody")
my_playlist.add_song("Stairway to Heaven")
my_playlist.show_songs()



Delete a method from a class:
class Person:
  def __init__(self, name):
    self.name = name

  def greet(self):
    print("Hello!")

p1 = Person("Emil")

del Person.greet

p1.greet()